{
  "catalog": "nnc-oser/modes@4",
  "modes": {
    "setup": {
      "status": "active",
      "summary": "Founder + Strategy Session + Root only. No governor, no workers, no subagents.",
      "active_roles": ["founder", "strategy-session", "root"],
      "subagents": "forbidden",
      "settings": { "permissions.deny": ["Agent", "Task", "Workflow"] }
    },
    "build": {
      "status": "admission-gated",
      "summary": "Root admits waves; one Governor session per wave (model family = profile) schedules fresh execution sessions; independent verification as required; only ROOT_ACCEPTED advances a milestone.",
      "active_roles": ["founder", "strategy-session", "root", "governor", "worker", "verifier"],
      "requires": ["operating_model", "build.admission=admitted"],
      "subagents": "per-packet",
      "settings": {}
    },
    "operate": {
      "status": "contract-only",
      "summary": "Contract only: the operating roster and gates are declared by the project when it goes live.",
      "active_roles": [],
      "requires": ["operating_model", "operate"],
      "subagents": "per-packet",
      "settings": {}
    }
  }
}
